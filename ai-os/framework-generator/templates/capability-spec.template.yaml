# Capability spec (shared template v0.9.1)
capability: "{{capability}}"
version: 0.1.0
target_generator: {{target_generator}}
modes: []
outputs: {}
registries:
  - workers
  - skills
  - schemas
semver: 0.1.0
