# Validator — review-engine-schema-check (v0.3.0)

Deterministic checks for Review Engine worker payloads. Output: `gate-review-report`.

Critical: schema, review fields, folder mirrors (findings + governance), score/status/decision coverage, RACI, no mutate/regenerate/validate-schemas/invent.
