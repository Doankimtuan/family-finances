# Validator — validation-engine-schema-check (v0.2.0)

Deterministic checks for Validation Engine **worker** payloads.

**Output artifact type:** `gate-validation-report` (not worker `validation-report`).

Critical: schema, finding fields, `folder_mirror` partition, score dimension coverage, report decision, no mutate/invent, evidence.
