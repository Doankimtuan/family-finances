# Validation Report (human mirror)

Machine source of truth is `validation-report` JSON under `artifacts/reports/`.

Include: summary, severity bands (critical→low), recommended-fix, PASS/FAIL decision.
