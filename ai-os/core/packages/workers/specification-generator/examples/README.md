# Examples — `specification-generator`

Illustrative only. Workers are **not executed** in this phase.

| File | Purpose |
|------|---------|
| `sample-input.json` | Claimed-run + consume inputs |
| `sample-output.json` | Envelope illustration |
| `sample-primary-payload.json` | `project-specification` with traceability + unknowns |
