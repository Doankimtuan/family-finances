# Execution surface (soft)

> Pipeline: `validation-engine` · Kind: consume (soft) · **Never modify source artifacts** · Report only

Soft notes on execution lifecycle / resume / incremental support expectations.

Runtime payloads: `core/packages/schemas/validation-engine-payload.schema.json` (`entries[]`).
Folder TEMPLATE is a human mirror.
