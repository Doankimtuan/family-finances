# Artifact index (soft)

> Pipeline: `validation-engine` · Kind: consume (soft) · **Never modify source artifacts** · Report only

Soft index over `runtime/artifacts/`. Do not invent. Do not mutate runtime store.

Runtime payloads: `schemas/validation-engine-payload.schema.json` (`entries[]`).
Folder TEMPLATE is a human mirror.
