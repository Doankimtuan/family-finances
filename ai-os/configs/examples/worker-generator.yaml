capability: Worker Generator
version: 0.1.0
target_generator: worker-generator
modes:
  - Worker README
  - Worker skill
  - Worker manifest
  - Examples scaffold pointers
outputs:
  worker_id: sample-worker
registries:
  - workers
  - skills
semver: 0.1.0
