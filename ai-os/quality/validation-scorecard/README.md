# quality/validation-scorecard

> **Additive** Validation Engine mirror under existing `quality/` (Solution Architecture `quality/` templates untouched).

Optional human-readable scorecard mirrors of `scores/` → `quality-scores`.
Validation Engine does **not** overwrite SA quality-notes.
