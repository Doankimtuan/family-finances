# Event Registry

No standalone approved CURRENT event registry was found during Phase 10 consolidation.

Future event registry updates must be made inside `artifacts/current/architecture/event-registry/` after approval through the current change policy.
