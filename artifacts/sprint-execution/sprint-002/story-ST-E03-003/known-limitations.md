# ST-E03-003 — Known Limitations

1. **Notifications prefs** — blueprint lists notifications; MVP ships profile + theme/locale + security only.
2. **Audit payloads** — UI shows timestamped “Policies updated”; detailed before/after JSON is stored but not rendered.
3. **Role promotion** — no UI to change partner ↔ admin (creator remains admin; invitees join as partner).
