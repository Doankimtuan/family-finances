# ST-E03-002 — Known Limitations

1. **No email send** — create invite returns a copyable deep link; outbound email is deferred.
2. **Couple limit** — hard-coded max 2 active partners (product Now assumption).
3. **Member email snapshot** — list uses join-time email column; not live Auth sync.
4. **Policies link** — deferred to `ST-E03-003` (prefs/account lifecycle remain on Together).
