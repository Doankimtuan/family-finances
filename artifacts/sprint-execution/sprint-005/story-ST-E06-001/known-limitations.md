# ST-E06-001 — Known Limitations

1. **Dismiss / acknowledge** — deferred to `ST-E06-002` (maturity/EMI guided flows).
2. **Jar movements** — resolve still sets `transactions.jar_id` without `jar_movements` rows.
3. **Authenticated e2e** — skips without `E2E_USER_*` / household.
4. **HeroUI Badge** — kind chip is a simple span on ReviewCard (catalog Badge is overlay-oriented).
